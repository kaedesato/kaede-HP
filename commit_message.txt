🧹 Code health: remove explicit any type in youtube utility

🎯 What
Add TypeScript interfaces for the YouTube API response and replace explicit `any` types used during JSON parsing.
💡 Why
Removes type-unsafe assertions, allowing TypeScript to accurately infer types and warn about potential runtime crashes.
✅ Verification
Verified via `pnpm check` and `pnpm test:unit`, ensuring both compile-time correctness and test passing.
✨ Result
Improved code health and type safety without changing behavior.
